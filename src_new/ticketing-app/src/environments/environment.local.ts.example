// Copy this file to environment.local.ts (gitignored) and fill in your keys.
// Run with: npm run start:local
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  googleMapsApiKey: 'YOUR_GOOGLE_MAPS_API_KEY_HERE',
  googleAuthEnabled: true, // Enable once GOOGLE_CLIENT_ID/SECRET are set in the API .env
  publicListingDomain: 'spindly.app'
};
