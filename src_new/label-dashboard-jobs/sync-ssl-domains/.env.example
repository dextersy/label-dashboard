# API Configuration
# Use the System API endpoint and system user credentials
API_BASE_URL=https://api.yourdomain.com
API_USERNAME=system@yourdomain.com
API_PASSWORD=your-system-password

# Notes:
# - API_USERNAME must be a system user email (is_system_user=true, brand_id=NULL)
# - The job uses /api/system/auth/login for authentication
# - The job fetches domains from /api/system/ssl-domains (DNS verification happens in API)
# - The job reads SSL certificate via /api/system/ssl-cert-domains (API does SSH)
# - The job removes domains via /api/system/ssl-domain/remove (API does SSH)
# - ALL SSH operations are handled by the backend API for security

# Email Notifications
SEND_SUCCESS_NOTIF=false
SEND_ERROR_NOTIF=true
ADMIN_EMAIL=admin@yourdomain.com
FROM_EMAIL=noreply@yourdomain.com

# SMTP Configuration (required if notifications enabled)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@gmail.com
SMTP_PASS=your-smtp-password
