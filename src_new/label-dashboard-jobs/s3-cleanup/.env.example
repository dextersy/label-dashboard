# System API Configuration
# Base URL of the Label Dashboard API
API_BASE_URL=https://api.example.com

# System User Credentials
# Use the System API endpoint and system user credentials
# API_USERNAME must be a system user email (is_system_user=true, brand_id=NULL)
API_USERNAME=system@yourdomain.com
API_PASSWORD=your-system-user-password

# Email Configuration
SUPERADMIN_EMAIL=superadmin@yourdomain.com
FROM_EMAIL=noreply@yourdomain.com

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=tls
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# AWS S3 Configuration
AWS_S3_BUCKET=your-bucket-name
AWS_S3_REGION=ap-southeast-1
AWS_S3_ACCESS_KEY=your-access-key
AWS_S3_SECRET_KEY=your-secret-key

# Cleanup Configuration
# Dry run mode - if 'true', will not delete files (only report what would be deleted)
DRY_RUN=true

# Minimum file age in days before considering for deletion (safety measure)
# Files newer than this will not be deleted even if unused
MIN_FILE_AGE_DAYS=7

# Notes:
# - API_USERNAME must be a system user (is_system_user=true, brand_id=NULL)
# - The job uses /api/system/auth/login for authentication
# - The job fetches all used URLs using /api/system/s3-used-urls
# - Set DRY_RUN=false to actually delete files
# - AWS credentials are managed through Lambda execution role or environment variables
