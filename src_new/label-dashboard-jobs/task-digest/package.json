{
  "name": "task-digest-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to send daily task digest emails and create in-app notifications for tasks due today and this week",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test:local": "npm run build && node dist/local-test.js",
    "package": "npm run build && bestzip task-digest.zip dist node_modules package.json",
    "deploy": "npm run package && aws lambda update-function-code --function-name task-digest --zip-file fileb://task-digest.zip"
  },
  "keywords": [
    "aws",
    "lambda",
    "tasks",
    "digest",
    "notifications"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.13.5",
    "nodemailer": "^8.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^7.0.0",
    "bestzip": "^2.2.1",
    "dotenv": "^16.4.5",
    "typescript": "^5.5.3"
  }
}
